apiVersion: v1
kind: Service
metadata:
  name: actual-budget
  labels:
    app.kubernetes.io/name: actual-budget
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: actual-budget
  ports:
    - name: http
      protocol: TCP
      port: 5006
      targetPort: http
